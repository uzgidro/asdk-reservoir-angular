apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: prod

resources:
  - ../../base
  - ingress.yaml

labels:
  - pairs:
      app.kubernetes.io/env: prod

images:
  - name: srmt-front
    newName: suxa/srmt-front
    newTag: latest

replicas:
  - name: srmt-front
    count: 2

patches:
  - patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: srmt-front
      spec:
        template:
          spec:
            containers:
              - name: srmt-front
                resources:
                  requests:
                    memory: "256Mi"
                    cpu: "200m"
                  limits:
                    memory: "512Mi"
                    cpu: "1000m"
